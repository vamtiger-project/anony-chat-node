var mongoose = require("mongoose"),
    Schema = require("vamtiger-database-schema"),
    schema = new Schema();

class Model {
    get DirectMessage() {
        return mongoose.model("directMessage", schema.directMessage);
    }
}

module.exports = Model;
